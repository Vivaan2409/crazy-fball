services:
  - type: web
    name: football-vision-ai
    runtime: docker
    plan: free
    region: oregon
    envVars:
      - key: FLASK_DEBUG
        value: "false"
      - key: SECRET_KEY
        generateValue: true
      - key: PORT
        value: "10000"
    autoDeploy: true
